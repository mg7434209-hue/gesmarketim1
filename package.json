{
  "name": "gesmarketim",
  "version": "2.0.0",
  "private": true,
  "description": "GES MARKETİM — solar e-ticaret (gesmarketim.com), React SPA + bağımlılıksız Node sunucu",
  "scripts": {
    "start": "node server.js",
    "build": "node build-catalog.js && node build-seo.js",
    "build:web": "cd frontend && npm run build",
    "test": "node -c server.js && node -c assets/config.js && node -c build-catalog.js && node -c build-seo.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
